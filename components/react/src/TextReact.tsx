import { createComponent } from "@lit/react";
import { Text } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const TextReact = createComponent({ tagName: "ft-text", elementClass: Text, react: React });
