import { createComponent } from "@lit/react";
import { Card } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const CardReact = createComponent({ tagName: "ft-card", elementClass: Card, react: React });
