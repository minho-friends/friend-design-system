import { createComponent } from "@lit/react";
import { Stack } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const StackReact = createComponent({ tagName: "ft-stack", elementClass: Stack, react: React });
