import { createComponent } from "@lit/react";
import { Metric } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const MetricReact = createComponent({ tagName: "ft-metric", elementClass: Metric, react: React });
