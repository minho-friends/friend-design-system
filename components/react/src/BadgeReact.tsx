import { createComponent } from "@lit/react";
import { Badge } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const BadgeReact = createComponent({ tagName: "ft-badge", elementClass: Badge, react: React });
