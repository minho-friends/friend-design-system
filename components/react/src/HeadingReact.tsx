import { createComponent } from "@lit/react";
import { Heading } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const HeadingReact = createComponent({ tagName: "ft-heading", elementClass: Heading, react: React });
