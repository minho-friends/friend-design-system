import { createComponent } from "@lit/react";
import { CollapsibleFooter } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const CollapsibleFooterReact = createComponent({
  tagName: "collapsible-footer",
  elementClass: CollapsibleFooter,
  react: React,
});
