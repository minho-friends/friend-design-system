import { createComponent } from "@lit/react";
import { Grid } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const GridReact = createComponent({ tagName: "ft-grid", elementClass: Grid, react: React });
