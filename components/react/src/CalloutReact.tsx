import { createComponent } from "@lit/react";
import { Callout } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const CalloutReact = createComponent({ tagName: "ft-callout", elementClass: Callout, react: React });
