import { createComponent } from "@lit/react";
import { Separator } from "@minho-friends/friend-design-system--lit";
import React from "react";

export const SeparatorReact = createComponent({ tagName: "ft-separator", elementClass: Separator, react: React });
